# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
*.local
*.tsbuildinfo

# expo / react-native
.expo/
.expo-shared/
apps/mobile/android/
apps/mobile/ios/
*.orig.*
web-build/

# secrets & local data (NEVER commit a user's tasks, db, or token)
.env
.env.*
!.env.example
**/data/
*.db
*.db-*
*.sqlite
*.sqlite-*

# android signing
*.keystore
*.jks

# TLS certs / private keys (e.g. from `tailscale cert`)
*.crt
*.key
*.pem

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Google Play service account key (never commit)
google-service-account*.json
**/google-service-account*.json
play-service-account*.json
releases/
